## 1. Migración de base de datos

- [x] 1.1 `prisma/schema.prisma` — modelo `EmitterFiscalSettings` agregado (`id String @id` sin default explícito, mismo patrón que `TicketSettings`/`PricingSettings`, no `@default("singleton")` como sugería design.md — el id fijo se setea en el repositorio, igual que `SINGLETON_ID` en `PrismaTicketSettingsRepository`). `rfc VARCHAR(13)`, `legalName VARCHAR(200)`, `fiscalRegime VARCHAR(3)`, `zipCode VARCHAR(5)`, todos nullable; `createdAt`/`updatedAt`.
- [x] 1.2 Migración aplicada contra Supabase en vivo. Nota operativa: se repitió el drift ya documentado en `carta-porte-from-sale` — `_prisma_migrations` no existía (confirmado vía Supabase MCP directo: tabla ausente en `information_schema.tables`, y las 40+ tablas existentes con 0 filas, incluida `users` — reset intencional de la BD de dev, confirmado por el usuario). Se hizo baseline de los 45 migrations previos (`migrate resolve --applied` uno por uno, solo metadata, sin re-ejecutar DDL — el primer loop de 45 se cortó por timeout de 2 min en el 43°, se completaron los 2 restantes en un segundo comando) y luego `migrate deploy` para aplicar la migración nueva (`20260819000001_add_emitter_fiscal_settings`, `CREATE TABLE emitter_fiscal_settings`). Verificado columna por columna vía `information_schema.columns` contra Supabase — coincide exactamente con el modelo Prisma.
- [x] 1.3 `npx prisma generate` corrido — cliente regenerado con `prisma.emitterFiscalSettings`.

## 2. Store compartido (shared infra)

- [x] 2.1 `src/shared/infrastructure/emitter/emitterFiscalSettingsStore.ts` creado: `EmitterFiscalData`/`PartialEmitterFiscalData`, `getEmitterFiscalSettings(tx?)`, `upsertEmitterFiscalSettings(data, tx?)` (upsert parcial sobre `SINGLETON_ID = "emitter-fiscal-settings-singleton"`, mismo patrón `SINGLETON_ID` que `PrismaTicketSettingsRepository`; sólo escribe campos presentes en `data`, spread condicional `!== undefined`), `isEmitterFiscalDataComplete(data): data is EmitterFiscalData` (type guard). `tx` parámetro opcional (`TxClient = PrismaClient | Prisma.TransactionClient`, default el singleton `prisma`) para permitir tests con mock y uso dentro de transacciones.
- [x] 2.2 `tests/unit/modules/shared/infrastructure/emitter/emitterFiscalSettingsStore.test.ts` — 7 tests contra un `tx` fake (mock in-memory, sin Prisma real, aprovechando el parámetro `tx` inyectable): `getEmitterFiscalSettings` null/con datos, `upsertEmitterFiscalSettings` create + upsert parcial no borra campos previos, `isEmitterFiscalDataComplete` null/parcial/completo. 7/7 en verde.

## 3. Backend — `billing` (CSD upload + persistencia)

- [x] 3.1 ~~`src/modules/billing/domain/errors.ts`: agregar `EmitterFiscalDataIncompleteError`~~ — **DESCARTADA (D5/D6 corregidos)**: se confirmó en código (grep exhaustivo, cero resultados) que `billing`'s `FacturamaRestGateway.stamp()` nunca leyó `FACTURAMA_EMITTER_*` ni construye un nodo `Emisor` — Facturama lo infiere del CSD ya cargado en la cuenta para CFDI Ingreso. Se agregó y luego se revirtió la clase de error + su import en `BillingController.ts` (código muerto, ninguna ruta la hubiera lanzado). Sólo `waybills` la necesita — ver 4.1.
- [x] 3.2 `src/modules/billing/infrastructure/http/BillingController.ts`: `csdSchema` gana `legalName: z.string().max(200).optional()`, `fiscalRegime: z.string().regex(/^\d{3}$/).optional()`, `zipCode: z.string().regex(/^\d{5}$/).optional()` (D3).
- [x] 3.3 `UploadCsdUseCase.execute`: nuevo tipo `UploadCsdRequest extends FacturamaCsdInput` con los 3 campos opcionales; tras `gateway.uploadCsd(input)` exitoso, llama `upsertEmitterFiscalSettings({rfc: input.rfc, legalName: input.legalName, fiscalRegime: input.fiscalRegime, zipCode: input.zipCode})` — campos `undefined` no se escriben (upsert parcial ya maneja esto). No persiste si `uploadCsd` lanza (el `await` previo ya interrumpe el flujo).
- [x] 3.4 `GetCsdStatusUseCase.execute`: nuevo tipo `CsdStatusWithFiscalData extends FacturamaCsdStatus`; `Promise.all([gateway.getCsdStatus(rfc), getEmitterFiscalSettings()])`, mergea `legalName`/`fiscalRegime`/`zipCode` (`?? null` si nunca capturados).
- [x] 3.5 ~~`FacturamaRestGateway.ts` (billing)~~ — **DESCARTADA**, ver 3.1. Sin cambios en este archivo.
- [x] 3.6 ~~Mapeo de `EmitterFiscalDataIncompleteError` en `BillingController`~~ — **DESCARTADA**, ver 3.1.

## 4. Backend — `waybills` (consumo del emisor)

- [x] 4.1 `src/modules/waybills/domain/errors.ts`: agregada `EmitterFiscalDataIncompleteError` (única, ya no duplicada con `billing` — D6 corregido).
- [x] 4.2 `FacturamaRestGateway.ts` (waybills): quitados `emitterRfc`/`emitterName`/`emitterFiscalRegime`/`emitterZipCode` del constructor (opts + lectura env + fail-fast); en `stampTraslado(...)`, `const emitter = await getEmitterFiscalSettings()` al inicio del método, `if (!isEmitterFiscalDataComplete(emitter)) throw new EmitterFiscalDataIncompleteError()` antes de armar el payload — `Receiver.Rfc/Name/FiscalRegime/TaxZipCode` y `ExpeditionPlace` ahora usan `emitter.rfc`/`emitter.legalName`/`emitter.fiscalRegime`/`emitter.zipCode`. Fail-fast de `FACTURAMA_USER`/`FACTURAMA_PASSWORD` sin cambio.
- [x] 4.3 `WaybillsController.ts`: import + mapeo de `EmitterFiscalDataIncompleteError` → HTTP 409 `{"error":"EmitterFiscalDataIncomplete"}`, junto a `FacturamaStampError`.

## 5. UI — `CsdManagerPage`

- [x] 5.1 `app/(private)/billing/_logic/types/api.ts`: `UploadCsdRequest` gana `legalName?`/`fiscalRegime?`/`zipCode?` (`string`); `CsdStatusDto` gana los mismos 3 (`string | null`).
- [x] 5.2 `uploadCsd.ts`/`getCsdStatus.ts` — no-op: son passthrough puro tipado contra `UploadCsdRequest`/`CsdStatusDto`; al extender los tipos en 5.1 los 3 campos ya viajan en el body/respuesta sin tocar el código de los servicios.
- [x] 5.3 `useCsdManager.ts`: `upload({ rfc, cerFile, keyFile, password, legalName?, fiscalRegime?, zipCode? })`, propagados al `uploadCsd(...)` interno; `status` ya expone los 3 vía el tipo `CsdStatusDto` extendido, sin cambio de código adicional.
- [x] 5.4 `CsdManagerPage.tsx`: 3 campos nuevos (razón social sin regex, régimen fiscal `maxLength=3` sólo dígitos, código postal `maxLength=5` sólo dígitos — mismos anchos que `customers`/`branches`), `useEffect` prellena desde `status` sólo si el campo local está vacío (no pisa lo que el usuario ya tecleó), 3 filas nuevas en "Estado actual", enviados como `undefined` si quedan vacíos (upsert parcial en backend los ignora).

## 6. Tests

- [x] 6.1 `tests/unit/modules/billing/UploadCsdUseCase.test.ts` (nuevo, 3 tests): éxito con los 3 campos → `upsertEmitterFiscalSettings` llamado con ellos; éxito sin los 3 campos → llamado con `undefined` en cada uno (upsert parcial los ignora); Facturama rechaza → `upsertEmitterFiscalSettings` NO llamado.
- [x] 6.2 `tests/unit/modules/billing/GetCsdStatusUseCase.test.ts` (nuevo, 2 tests): merge correcto de status Facturama + settings persistidos; settings nunca capturados (`null`) → los 3 campos `null` en la respuesta.
- [x] 6.3 `tests/unit/modules/waybills/FacturamaRestGateway.test.ts` (reescrito, sólo `waybills` — `billing`'s gateway no cambia, ver 3.1): quitados `emitterRfc` de `BASE_OPTS` y el test de fail-fast en construcción; agregado `jest.mock` del store compartido; 3 tests nuevos: construcción ya no falla por emisor faltante, `stampTraslado` con settings incompletos → `EmitterFiscalDataIncompleteError` sin llamar `fetchImpl`, mismo con settings `null`. Los tests de `Receiver`/payload ya existentes pasan sin cambio de aserciones (mock devuelve los mismos valores que antes venían de env). 10/10 en verde.
- [x] 6.4 `tests/unit/modules/billing/infrastructure/http/BillingControllerCsd.test.ts` (nuevo, 4 tests): `csdSchema` acepta con/sin los 3 campos opcionales, rechaza `fiscalRegime`/`zipCode` con formato inválido (400, sin llamar `upsertEmitterFiscalSettings`). Sin caso de `EmitterFiscalDataIncomplete` (no aplica a `billing`, ver 3.1).
- [x] 6.5 `tests/unit/modules/waybills/WaybillsController.emitterFiscalData.test.ts` (nuevo, 1 test): gateway fake cuyo `stampTraslado` lanza `EmitterFiscalDataIncompleteError` → `POST /waybills` (carta_porte) responde 409 `{"error":"EmitterFiscalDataIncomplete"}`.
- [x] 6.6 `tests/unit/ui/(private)/billing/CsdManagerPage.test.tsx` (nuevo, 3 tests): los 3 campos nuevos se renderizan, se prellenan desde `status` (mock de `useCsdManager`), y viajan en el `upload(...)` del submit.

## 7. Verificación final

- [x] 7.1 `npm test`: 43 fallos iniciales, TODOS en `tests/integration/` por `"No Role found"`/seed vacío — consecuencia directa del reset de BD detectado en 1.2, no relacionado al código de este change (0 unit tests afectados: 476/476 suites unitarias, 3378/3378 tests unitarios en verde desde el primer run). Se corrió `npm run seed` + `npm run seed:folios` para restaurar RBAC/folios canónicos (idempotentes, mismo patrón documentado en CLAUDE.md) — re-run de los 34 suites de integración: **34/34 en verde, 223/223 tests**. Suite completa: **510/510 suites, 3601/3601 tests en verde**.
- [x] 7.2 `npm run build` — falló primero por versión de Node activa en shell (v18.0.0, < mínimo v18.17.0 de Next.js); cambiado a Node v24.14.1 (`.nvmrc` del repo) vía `nvm use`, no relacionado a este change. Build exitoso, incluye `/billing/csd` (6.55 kB) y `/sales/[id]/waybill/new` (5.6 kB) compiladas. `npx tsc --noEmit -p .` sin errores nuevos — mismos 9 pre-existentes documentados en `carta-porte-from-sale`.
- [x] 7.3 Grep de `FACTURAMA_EMITTER_` en `src/modules/waybills/` y `src/modules/billing/`: **cero resultados** — confirmado limpio.
- [x] 7.4 `.env.example`: las 4 vars `FACTURAMA_EMITTER_*` comentadas (`#`) con nota explicando que el emisor ahora se captura vía `/billing` → `emitter_fiscal_settings`; valores de ejemplo preservados, sin remover las líneas.
- [ ] 7.5 Manual (requiere `FACTURAMA_MOCK=false` + sandbox, sesión de navegador): subir CSD + datos fiscales en `/billing`, confirmar persistencia (`GET /csd` refleja lo guardado tras recargar), timbrar una factura y una Carta Porte de venta y confirmar que el nodo `Emisor` usa los datos de BD (no falla con `EmitterFiscalDataIncompleteError`).
